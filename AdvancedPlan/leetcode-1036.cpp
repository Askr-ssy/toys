#include<iostream>
#include<vector>
using namespace std;


class Solution {
public:
    bool isEscapePossible(vector<vector<int>>& blocked, vector<int>& source, vector<int>& target) {

    }
};

int main(void){
    Solution temp{};
    vector<vector<int>> input_data{};
    vector<int> source{};
    vector<int> target{}; 
    cout<<temp.isEscapePossible(input_data,source,target);
}